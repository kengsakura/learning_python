// เนื้อหาบทเรียน + แบบทดสอบ — แก้ไฟล์นี้แล้ว "เพิ่ม SEED_VERSION" เพื่อให้ระบบ sync ลงฐานข้อมูล
// (โจทย์เขียนโปรแกรมอยู่ใน seed-problems.json — สร้างจาก scripts/gen_problems.py)
export const SEED_VERSION = 2;

export type SeedQuestion = [question: string, choices: string[], answerIndex: number, explanation: string];
export type SeedLesson = { title: string; content: string; questions: SeedQuestion[] };

export const LESSONS: SeedLesson[] = [
  {
    title: "รู้จัก Python และคำสั่ง print",
    content: `# รู้จัก Python และคำสั่ง print

Python เป็นภาษาโปรแกรมที่อ่านง่ายที่สุดภาษาหนึ่ง ใช้ทำได้ตั้งแต่เว็บไซต์ เกม วิเคราะห์ข้อมูล ไปจนถึง AI โค้ด Python อ่านคล้ายภาษาอังกฤษ จึงเหมาะมากสำหรับผู้เริ่มต้น

## คำสั่ง print แสดงผลข้อความ

\`\`\`python
print("สวัสดี Python")
print("ผมชื่อ", "เพทาย")
print(1 + 2)
\`\`\`

ผลลัพธ์:

\`\`\`
สวัสดี Python
ผมชื่อ เพทาย
3
\`\`\`

## จุดสังเกต
- ข้อความ (string) ต้องอยู่ในเครื่องหมายคำพูด \`"..."\` หรือ \`'...'\`
- ตัวเลขไม่ต้องใส่เครื่องหมายคำพูด และคำนวณได้เลย
- ใส่หลายค่าใน print ได้ คั่นด้วย \`,\` — Python จะเว้นวรรคให้อัตโนมัติ
- \`print()\` ขึ้นบรรทัดใหม่ให้อัตโนมัติทุกครั้ง

## ปรับแต่ง print ด้วย sep และ end

\`\`\`python
print("A", "B", "C", sep="-")   # A-B-C
print("ไม่ขึ้นบรรทัดใหม่", end="")
print(" ต่อท้ายเลย")
\`\`\`

## อักขระพิเศษ

| เขียน | ความหมาย |
|-------|----------|
| \\\\n | ขึ้นบรรทัดใหม่ |
| \\\\t | แท็บ (เว้นวรรคยาว) |
| \\\\" | เครื่องหมายคำพูดในข้อความ |

\`\`\`python
print("บรรทัดแรก\\nบรรทัดสอง")
\`\`\`

## คอมเมนต์ (comment)

ใช้ \`#\` เขียนหมายเหตุ โปรแกรมจะไม่ทำงานบรรทัดนั้น ช่วยให้คนอ่านโค้ดเข้าใจง่ายขึ้น

\`\`\`python
# บรรทัดนี้คือคอมเมนต์
print("ทำงานเฉพาะบรรทัดนี้")  # คอมเมนต์ท้ายบรรทัดก็ได้
\`\`\`

ลองนำโค้ดไปรันในหน้า **โจทย์ฝึก** ได้เลย!`,
    questions: [
      ["คำสั่งใดใช้แสดงข้อความออกทางหน้าจอ?", ["show()", "print()", "echo()", "display()"], 1, "Python ใช้ print() ในการแสดงผล"],
      ["print(2 + 3) จะแสดงผลอะไร?", ["2 + 3", "5", "23", "เกิดข้อผิดพลาด"], 1, "ตัวเลขไม่อยู่ในเครื่องหมายคำพูด จึงถูกคำนวณก่อนแสดงผล"],
      ["สัญลักษณ์ใดใช้เขียนคอมเมนต์?", ["//", "<!--", "#", "%%"], 2, "Python ใช้ # นำหน้าคอมเมนต์"],
      ['print("A", "B", sep="*") แสดงอะไร?', ["A B", "A*B", "AB", "A,B"], 1, "sep กำหนดตัวคั่นระหว่างค่า แทนช่องว่างปกติ"],
      ["อักขระใดทำให้ขึ้นบรรทัดใหม่?", ["\\t", "\\s", "\\n", "\\b"], 2, "\\n คือ newline ขึ้นบรรทัดใหม่"],
    ],
  },
  {
    title: "ตัวแปรและชนิดข้อมูล",
    content: `# ตัวแปรและชนิดข้อมูล

ตัวแปร (variable) คือชื่อที่ใช้เก็บค่า เปรียบเหมือนกล่องติดป้ายชื่อ เก็บค่าไว้ใช้ภายหลังได้

\`\`\`python
name = "มะลิ"
age = 14
height = 158.5
is_student = True
\`\`\`

## กติกาการตั้งชื่อตัวแปร
- ใช้ตัวอักษร ตัวเลข และ \`_\` ได้ แต่**ห้ามขึ้นต้นด้วยตัวเลข**
- ตัวพิมพ์เล็ก-ใหญ่ถือว่าต่างกัน (\`score\` กับ \`Score\` คนละตัว)
- ตั้งชื่อให้สื่อความหมาย เช่น \`total_price\` ดีกว่า \`x\`

## ชนิดข้อมูลพื้นฐาน

| ชนิด | ตัวอย่าง | ความหมาย |
|------|----------|----------|
| int | 14 | จำนวนเต็ม |
| float | 158.5 | ทศนิยม |
| str | "มะลิ" | ข้อความ |
| bool | True / False | ค่าจริง/เท็จ |

ตรวจชนิดข้อมูลได้ด้วย \`type()\`:

\`\`\`python
print(type(14))      # <class 'int'>
print(type("14"))    # <class 'str'>
\`\`\`

## การรับข้อมูลด้วย input

\`input()\` รับข้อมูลจากผู้ใช้ และได้ค่าเป็น **ข้อความ (str) เสมอ** ถ้าต้องการตัวเลขให้แปลงด้วย \`int()\` หรือ \`float()\`

\`\`\`python
name = input()
age = int(input())
print("สวัสดี", name, "อายุ", age, "ปี")
\`\`\`

## แปลงชนิดข้อมูล

\`\`\`python
int("42")      # 42
float("3.5")   # 3.5
str(99)        # "99"
int(7.9)       # 7  (ตัดทศนิยมทิ้ง ไม่ปัดเศษ)
\`\`\`

## f-string จัดรูปแบบข้อความ

วิธีที่สะดวกที่สุดในการรวมตัวแปรเข้ากับข้อความ: เติม \`f\` หน้าเครื่องหมายคำพูด แล้วใส่ตัวแปรใน \`{}\`

\`\`\`python
score = 95
print(f"คุณได้ {score} คะแนน")
print(f"สองเท่าคือ {score * 2}")     # คำนวณใน {} ได้เลย
pi = 3.14159
print(f"ค่าพาย ≈ {pi:.2f}")          # ทศนิยม 2 ตำแหน่ง → 3.14
\`\`\``,
    questions: [
      ["input() คืนค่าเป็นชนิดข้อมูลใดเสมอ?", ["int", "float", "str", "bool"], 2, "input() ได้ค่าเป็นข้อความ ต้องแปลงเองถ้าจะใช้เป็นตัวเลข"],
      ["ข้อใดเป็นการแปลงข้อความเป็นจำนวนเต็ม?", ["str(x)", "int(x)", "float(x)", "bool(x)"], 1, "int() แปลงเป็นจำนวนเต็ม"],
      ["ชื่อตัวแปรใด**ผิด**กติกา?", ["my_score", "score2", "2score", "_temp"], 2, "ชื่อตัวแปรห้ามขึ้นต้นด้วยตัวเลข"],
      ["int(7.9) ได้ค่าอะไร?", ["8", "7.9", "7", "เกิดข้อผิดพลาด"], 2, "int() ตัดทศนิยมทิ้ง ไม่ใช่ปัดเศษ"],
      ['print(f"ได้ {2+3} คะแนน") แสดงอะไร?', ["ได้ {2+3} คะแนน", "ได้ 5 คะแนน", "ได้ 2+3 คะแนน", "เกิดข้อผิดพลาด"], 1, "f-string คำนวณนิพจน์ในวงเล็บปีกกาให้"],
    ],
  },
  {
    title: "ตัวดำเนินการและการคำนวณ",
    content: `# ตัวดำเนินการและการคำนวณ

## ตัวดำเนินการคณิตศาสตร์

| สัญลักษณ์ | ความหมาย | ตัวอย่าง | ผลลัพธ์ |
|-----------|----------|----------|---------|
| + | บวก | 7 + 3 | 10 |
| - | ลบ | 7 - 3 | 4 |
| * | คูณ | 7 * 3 | 21 |
| / | หาร (ได้ทศนิยมเสมอ) | 7 / 2 | 3.5 |
| // | หารปัดเศษทิ้ง | 7 // 2 | 3 |
| % | เศษจากการหาร (mod) | 7 % 2 | 1 |
| ** | ยกกำลัง | 2 ** 10 | 1024 |

## % กับ // มีประโยชน์มาก!

\`\`\`python
n = 137
print(n % 10)    # 7  → หลักหน่วย
print(n // 10)   # 13 → ตัดหลักหน่วยทิ้ง
print(n % 2)     # 1  → เป็น 1 แปลว่าเลขคี่

minutes = 130
print(minutes // 60, "ชั่วโมง", minutes % 60, "นาที")  # 2 ชั่วโมง 10 นาที
\`\`\`

## ลำดับการคำนวณ

เหมือนคณิตศาสตร์: วงเล็บ → ยกกำลัง → คูณ/หาร → บวก/ลบ

\`\`\`python
print(2 + 3 * 4)     # 14 ไม่ใช่ 20
print((2 + 3) * 4)   # 20
\`\`\`

## ตัวดำเนินการกำหนดค่าแบบย่อ

\`\`\`python
score = 10
score += 5    # score = score + 5  → 15
score -= 3    # → 12
score *= 2    # → 24
\`\`\`

## คำนวณกับข้อความก็ได้

\`\`\`python
print("ab" + "cd")    # abcd  (ต่อข้อความ)
print("ha" * 3)       # hahaha (ทำซ้ำ)
\`\`\`

> ระวัง: \`"5" + "3"\` ได้ \`"53"\` (ต่อข้อความ) ไม่ใช่ 8 — ถ้าจะบวกเลขต้องแปลงเป็น int ก่อน`,
    questions: [
      ["7 // 2 ได้ค่าอะไร?", ["3.5", "3", "4", "1"], 1, "// คือหารแบบปัดเศษทิ้ง"],
      ["10 % 3 ได้ค่าอะไร?", ["3", "0", "1", "3.33"], 2, "% คือเศษจากการหาร 10 ÷ 3 เหลือเศษ 1"],
      ["2 + 3 * 4 ได้ค่าอะไร?", ["20", "14", "24", "9"], 1, "คูณมาก่อนบวก: 3*4=12 แล้ว 2+12=14"],
      ['"5" + "3" ได้ค่าอะไร?', ["8", "53", "เกิดข้อผิดพลาด", "5 3"], 1, "เครื่องหมาย + กับข้อความคือการต่อข้อความ"],
      ["นิพจน์ใดใช้ตรวจว่า n เป็นเลขคู่?", ["n / 2 == 0", "n % 2 == 0", "n // 2 == 0", "n * 2 == 0"], 1, "เลขคู่หาร 2 ลงตัว เศษเป็น 0"],
    ],
  },
  {
    title: "เงื่อนไข if / elif / else",
    content: `# เงื่อนไข if / elif / else

ใช้ตัดสินใจว่าจะทำคำสั่งไหน ตามเงื่อนไขที่เป็นจริง

\`\`\`python
score = int(input())
if score >= 80:
    print("เกรด A")
elif score >= 70:
    print("เกรด B")
elif score >= 60:
    print("เกรด C")
else:
    print("ต้องพยายามอีกนิด")
\`\`\`

โปรแกรมตรวจเงื่อนไขจากบนลงล่าง เจอข้อแรกที่จริงก็ทำข้อนั้นแล้ว**ข้ามที่เหลือทั้งหมด**

> **สำคัญ:** บรรทัดในบล็อก if ต้องย่อหน้า (indent) เท่ากัน ปกติใช้ 4 เว้นวรรค — Python ใช้ย่อหน้าแบ่งบล็อกแทนวงเล็บปีกกา

## ตัวดำเนินการเปรียบเทียบ

| สัญลักษณ์ | ความหมาย |
|-----------|----------|
| == | เท่ากับ (ระวัง: = ตัวเดียวคือกำหนดค่า) |
| != | ไม่เท่ากับ |
| > , < | มากกว่า, น้อยกว่า |
| >= , <= | มากกว่าหรือเท่ากับ, น้อยกว่าหรือเท่ากับ |

เปรียบเทียบต่อกันได้แบบคณิตศาสตร์: \`10 <= n <= 99\` แปลว่า n อยู่ระหว่าง 10 ถึง 99

## เชื่อมหลายเงื่อนไขด้วย and / or / not

\`\`\`python
age = 15
if age >= 13 and age <= 19:
    print("เป็นวัยรุ่น")

day = "เสาร์"
if day == "เสาร์" or day == "อาทิตย์":
    print("วันหยุด!")
\`\`\`

- \`and\` จริงทั้งคู่จึงจริง
- \`or\` จริงอย่างน้อยหนึ่งก็จริง
- \`not\` กลับค่าจริงเป็นเท็จ

## if ซ้อน if

\`\`\`python
if score >= 50:
    if score >= 80:
        print("ผ่านแบบยอดเยี่ยม")
    else:
        print("ผ่าน")
else:
    print("ไม่ผ่าน")
\`\`\`

## เขียนแบบย่อ (conditional expression)

\`\`\`python
n = int(input())
result = "even" if n % 2 == 0 else "odd"
print(result)
\`\`\``,
    questions: [
      ["สัญลักษณ์ใดใช้เปรียบเทียบว่า 'เท่ากับ'?", ["=", "==", "!=", "equals"], 1, "= ใช้กำหนดค่า ส่วน == ใช้เปรียบเทียบ"],
      ["x = 10 แล้ว x > 5 and x < 8 ได้ค่าอะไร?", ["True", "False", "10", "เกิดข้อผิดพลาด"], 1, "10 < 8 เป็นเท็จ and จึงได้ False"],
      ["ถ้าเงื่อนไข if เป็นเท็จ และมี else โปรแกรมจะทำอย่างไร?", ["หยุดทำงาน", "ทำบล็อก if อยู่ดี", "ทำบล็อก else", "ข้ามทั้งหมด"], 2, "else ทำงานเมื่อเงื่อนไขก่อนหน้าเป็นเท็จทั้งหมด"],
      ["Python ใช้อะไรแบ่งบล็อกของ if?", ["วงเล็บปีกกา {}", "คำว่า begin/end", "การย่อหน้า (indent)", "เครื่องหมาย ;"], 2, "Python ใช้ย่อหน้าเป็นตัวกำหนดบล็อก ต่างจากหลายภาษา"],
      ["เงื่อนไขใดตรวจว่า n อยู่ระหว่าง 1 ถึง 10?", ["n >= 1 or n <= 10", "1 <= n <= 10", "n == 1-10", "n in 1..10"], 1, "Python เขียนเปรียบเทียบต่อกันได้ หรือใช้ n >= 1 and n <= 10"],
    ],
  },
  {
    title: "ลูป for และ while",
    content: `# ลูป for และ while

ลูป (loop) ใช้ทำคำสั่งซ้ำ ๆ โดยไม่ต้องเขียนหลายรอบ

## ลูป for กับ range

\`\`\`python
for i in range(5):
    print(i)        # 0 1 2 3 4

for i in range(1, 6):
    print(i)        # 1 2 3 4 5

for i in range(10, 0, -2):
    print(i)        # 10 8 6 4 2
\`\`\`

- \`range(n)\` นับ 0 ถึง n-1
- \`range(a, b)\` นับ a ถึง b-1 (ไม่รวม b!)
- \`range(a, b, step)\` นับทีละ step — step ติดลบคือนับถอยหลัง

## for วนอ่านข้อความหรือลิสต์ได้โดยตรง

\`\`\`python
for ch in "abc":
    print(ch)            # a b c ทีละบรรทัด

for fruit in ["ส้ม", "กล้วย"]:
    print(fruit)
\`\`\`

## ลูป while

ทำซ้ำตราบใดที่เงื่อนไขเป็นจริง — เหมาะเมื่อไม่รู้จำนวนรอบล่วงหน้า

\`\`\`python
n = 1
while n <= 5:
    print(n)
    n = n + 1
\`\`\`

> **ระวังลูปไม่จบ!** ถ้าลืมเพิ่มค่า n ลูปจะวนตลอดไป

## break และ continue

\`\`\`python
for i in range(10):
    if i == 5:
        break       # ออกจากลูปทันที
    if i % 2 == 0:
        continue    # ข้ามรอบนี้ไปรอบถัดไป
    print(i)        # 1 3
\`\`\`

## รูปแบบที่ใช้บ่อย: ตัวสะสม (accumulator)

\`\`\`python
n = int(input())
total = 0
for i in range(1, n + 1):
    total += i
print(total)         # ผลรวม 1 ถึง n
\`\`\`

## ลูปซ้อนลูป

\`\`\`python
for i in range(1, 4):
    for j in range(1, 4):
        print(i, j)   # (1,1) (1,2) (1,3) (2,1) ... (3,3)
\`\`\`

ใช้วาดตาราง สี่เหลี่ยมดาว หรือเทียบสมาชิกทุกคู่`,
    questions: [
      ["range(3) ให้ค่าอะไรบ้าง?", ["1, 2, 3", "0, 1, 2", "0, 1, 2, 3", "3"], 1, "range(n) เริ่มที่ 0 ถึง n-1"],
      ["คำสั่งใดใช้ออกจากลูปทันที?", ["stop", "exit", "break", "continue"], 2, "break ออกจากลูป ส่วน continue ข้ามรอบ"],
      ["while ต่างจาก for อย่างไร?", ["ทำซ้ำตามเงื่อนไข", "ทำซ้ำตามจำนวนรอบเท่านั้น", "ใช้กับข้อความไม่ได้", "เร็วกว่าเสมอ"], 0, "while ทำซ้ำตราบใดที่เงื่อนไขยังจริง"],
      ["range(2, 10, 2) ให้ค่าอะไรบ้าง?", ["2 4 6 8 10", "2 4 6 8", "2 3 4 ... 10", "4 6 8"], 1, "เริ่ม 2 เพิ่มทีละ 2 ไม่รวม 10"],
      ["โค้ด while True: print(1) จะเกิดอะไรขึ้น?", ["พิมพ์ 1 ครั้งเดียว", "เกิด error", "วนไม่จบ", "ไม่ทำอะไร"], 2, "เงื่อนไขจริงตลอด ลูปจึงไม่มีวันจบ — ต้องมี break หรือแก้เงื่อนไข"],
    ],
  },
  {
    title: "ข้อความ (String)",
    content: `# ข้อความ (String)

ข้อความใน Python เป็นลำดับของตัวอักษร เข้าถึงทีละตัวหรือตัดเป็นช่วงได้

## index และ slicing

\`\`\`python
s = "python"
print(s[0])      # p   (index เริ่มที่ 0)
print(s[-1])     # n   (ติดลบนับจากท้าย)
print(s[0:3])    # pyt (ตั้งแต่ 0 ถึงก่อน 3)
print(s[2:])     # thon
print(s[:4])     # pyth
print(s[::-1])   # nohtyp (กลับข้อความ!)
print(len(s))    # 6
\`\`\`

## เมธอดที่ใช้บ่อย

| เมธอด | ทำอะไร | ตัวอย่าง |
|-------|--------|----------|
| upper() / lower() | พิมพ์ใหญ่/เล็ก | "Hi".upper() → "HI" |
| strip() | ตัดช่องว่างหัวท้าย | " hi ".strip() → "hi" |
| count(x) | นับว่ามี x กี่ครั้ง | "banana".count("a") → 3 |
| replace(a, b) | แทนที่ a ด้วย b | "cat".replace("c", "b") → "bat" |
| split() | ตัดเป็นลิสต์ | "a b c".split() → ["a","b","c"] |
| join(list) | รวมลิสต์เป็นข้อความ | " ".join(["a","b"]) → "a b" |
| startswith(x) | ขึ้นต้นด้วย x ไหม | "python".startswith("py") → True |
| isdigit() | เป็นตัวเลขล้วนไหม | "123".isdigit() → True |

## ตรวจว่ามีคำอยู่ในข้อความไหม

\`\`\`python
sentence = "I love Python"
if "Python" in sentence:
    print("พบคำว่า Python!")
\`\`\`

## วนอ่านทีละตัวอักษร

\`\`\`python
count = 0
for ch in "Hello World":
    if ch in "aeiou":
        count += 1
print(count)    # นับสระ
\`\`\`

> **ข้อควรรู้:** string แก้ไขตัวอักษรข้างในตรง ๆ ไม่ได้ (\`s[0] = "x"\` จะ error) — ต้องสร้างข้อความใหม่แทน เช่นใช้ replace หรือต่อข้อความใหม่`,
    questions: [
      ['s = "python" แล้ว s[1] คืออะไร?', ["p", "y", "t", "เกิดข้อผิดพลาด"], 1, "index เริ่มที่ 0 ดังนั้น s[1] คือตัวที่สอง"],
      ['"hello"[::-1] ได้อะไร?', ["hello", "olleh", "h", "o"], 1, "[::-1] คือการอ่านย้อนกลับ"],
      ['"a b c".split() ได้อะไร?', ['"abc"', '["a b c"]', '["a", "b", "c"]', "3"], 2, "split() ตัดข้อความตามช่องว่างเป็นลิสต์"],
      ['"banana".count("a") ได้ค่าอะไร?', ["2", "3", "1", "6"], 1, "มีตัว a สามตัวใน banana"],
      ["ข้อใดตรวจว่า text ขึ้นต้นด้วย 'py'?", ['text.begins("py")', 'text.startswith("py")', '"py" == text', 'text.first("py")'], 1, "ใช้เมธอด startswith()"],
    ],
  },
  {
    title: "ลิสต์ (List)",
    content: `# ลิสต์ (List)

ลิสต์เก็บข้อมูลหลายค่าไว้ในตัวแปรเดียว เรียงตามลำดับ แก้ไขได้

\`\`\`python
fruits = ["ส้ม", "กล้วย", "แอปเปิล"]
print(fruits[0])      # ส้ม (index เริ่มที่ 0)
print(fruits[-1])     # แอปเปิล (นับจากท้าย)
print(len(fruits))    # 3
fruits[1] = "มะม่วง"   # แก้ไขค่าได้
\`\`\`

## เพิ่ม/ลบสมาชิก

\`\`\`python
fruits.append("องุ่น")      # เพิ่มท้ายลิสต์
fruits.insert(0, "ทุเรียน")  # แทรกที่ตำแหน่ง 0
fruits.remove("กล้วย")      # ลบค่าที่ระบุ (ตัวแรกที่เจอ)
last = fruits.pop()         # หยิบตัวสุดท้ายออก
\`\`\`

## slicing ก็ใช้กับลิสต์ได้

\`\`\`python
nums = [10, 20, 30, 40, 50]
print(nums[1:4])    # [20, 30, 40]
print(nums[::-1])   # [50, 40, 30, 20, 10]
\`\`\`

## วนลูปอ่านลิสต์

\`\`\`python
scores = [80, 95, 60]
total = 0
for s in scores:
    total += s
print(total / len(scores))   # ค่าเฉลี่ย
\`\`\`

## ฟังก์ชันที่ใช้บ่อย

| ฟังก์ชัน | ความหมาย |
|----------|----------|
| len(lst) | จำนวนสมาชิก |
| sum(lst) | ผลรวม |
| max(lst), min(lst) | ค่ามาก/น้อยสุด |
| sorted(lst) | คืนลิสต์ใหม่ที่เรียงแล้ว |
| lst.sort() | เรียงลิสต์เดิม (แก้ของเดิม) |
| lst.index(x) | ตำแหน่งแรกที่พบ x |
| x in lst | ตรวจว่ามี x ไหม (True/False) |

## รับข้อมูลเป็นลิสต์

ท่ามาตรฐานของโจทย์เขียนโปรแกรม:

\`\`\`python
nums = [int(x) for x in input().split()]
print(max(nums))
\`\`\`

## list comprehension

สร้างลิสต์ใหม่จากลิสต์เดิมแบบสั้น ๆ:

\`\`\`python
nums = [1, 2, 3, 4, 5, 6]
doubled = [x * 2 for x in nums]            # [2, 4, 6, 8, 10, 12]
evens = [x for x in nums if x % 2 == 0]    # [2, 4, 6]
\`\`\``,
    questions: [
      ['fruits = ["ส้ม", "กล้วย"] แล้ว fruits[1] คืออะไร?', ["ส้ม", "กล้วย", "1", "เกิดข้อผิดพลาด"], 1, "index เริ่มนับที่ 0 ดังนั้น [1] คือตัวที่สอง"],
      ["คำสั่งใดเพิ่มสมาชิกต่อท้ายลิสต์?", ["add()", "push()", "insert()", "append()"], 3, "append() เพิ่มต่อท้ายลิสต์"],
      ["sum([1, 2, 3]) ได้ค่าอะไร?", ["123", "6", "[6]", "3"], 1, "sum รวมค่าทุกตัวในลิสต์"],
      ["sorted(lst) ต่างจาก lst.sort() อย่างไร?", ["ไม่ต่างกัน", "sorted คืนลิสต์ใหม่ ไม่แก้ของเดิม", "sort เร็วกว่า", "sorted ใช้กับตัวเลขไม่ได้"], 1, "sorted() คืนสำเนาที่เรียงแล้ว ส่วน .sort() เรียงลิสต์เดิม"],
      ["[x * 2 for x in [1, 2, 3]] ได้อะไร?", ["[1, 2, 3, 1, 2, 3]", "[2, 4, 6]", "[1, 4, 9]", "6"], 1, "list comprehension คูณแต่ละตัวด้วย 2"],
    ],
  },
  {
    title: "ดิกชันนารี (Dictionary)",
    content: `# ดิกชันนารี (Dictionary)

dictionary เก็บข้อมูลเป็นคู่ **key → value** เหมือนพจนานุกรมที่เปิดหาคำ (key) เพื่อดูความหมาย (value)

\`\`\`python
student = {"name": "มะลิ", "age": 14, "score": 95}
print(student["name"])    # มะลิ
student["age"] = 15        # แก้ค่า
student["grade"] = "A"     # เพิ่ม key ใหม่
\`\`\`

## เข้าถึงอย่างปลอดภัยด้วย get

ถ้า key ไม่มีอยู่ \`student["height"]\` จะ error แต่ \`get\` จะคืนค่า default แทน

\`\`\`python
print(student.get("height", 0))   # 0 (ไม่มี key นี้)
\`\`\`

## ตรวจว่ามี key ไหม

\`\`\`python
if "score" in student:
    print("มีคะแนนแล้ว")
\`\`\`

## วนลูปอ่าน dictionary

\`\`\`python
prices = {"ส้ม": 20, "กล้วย": 10, "แอปเปิล": 30}

for fruit in prices:                # วนตาม key
    print(fruit, prices[fruit])

for fruit, price in prices.items(): # ได้ทั้ง key และ value
    print(f"{fruit} ราคา {price} บาท")
\`\`\`

## ตัวอย่างยอดฮิต: นับความถี่

\`\`\`python
text = "banana"
count = {}
for ch in text:
    count[ch] = count.get(ch, 0) + 1
print(count)    # {'b': 1, 'a': 3, 'n': 2}
\`\`\`

## เมธอดที่ใช้บ่อย

| คำสั่ง | ความหมาย |
|--------|----------|
| d.keys() | key ทั้งหมด |
| d.values() | value ทั้งหมด |
| d.items() | คู่ (key, value) ทั้งหมด |
| d.get(k, default) | อ่านค่าแบบไม่ error |
| del d[k] | ลบ key |
| len(d) | จำนวนคู่ |`,
    questions: [
      ['d = {"a": 1, "b": 2} แล้ว d["b"] คืออะไร?', ["b", "1", "2", "เกิดข้อผิดพลาด"], 2, "เข้าถึง value ผ่าน key"],
      ["ถ้า key ไม่มีอยู่ การใช้ d[key] จะเกิดอะไร?", ["ได้ None", "ได้ 0", "เกิด error", "สร้าง key ใหม่"], 2, "เข้าถึง key ที่ไม่มีอยู่จะเกิด KeyError — ใช้ get() เพื่อเลี่ยง"],
      ['d.get("x", 5) คืนอะไรถ้าไม่มี key "x"?', ["error", "None", "5", '"x"'], 2, "get คืนค่า default ที่กำหนด เมื่อไม่พบ key"],
      ["คำสั่งใดได้ทั้ง key และ value พร้อมกันตอนวนลูป?", ["d.all()", "d.items()", "d.pairs()", "d.both()"], 1, "for k, v in d.items()"],
      ['count[ch] = count.get(ch, 0) + 1 ใช้ทำอะไร?', ["ลบตัวอักษร", "นับความถี่ตัวอักษร", "เรียงตัวอักษร", "สุ่มตัวอักษร"], 1, "ท่ามาตรฐานของการนับความถี่ด้วย dictionary"],
    ],
  },
  {
    title: "ทูเพิลและเซ็ต (Tuple & Set)",
    content: `# ทูเพิลและเซ็ต (Tuple & Set)

## ทูเพิล (tuple) — ลิสต์ที่แก้ไขไม่ได้

\`\`\`python
point = (3, 5)
print(point[0])    # 3
# point[0] = 9     # ❌ error! tuple แก้ค่าไม่ได้
\`\`\`

ใช้เมื่อข้อมูลไม่ควรถูกแก้ เช่น พิกัด วันที่ และใช้สลับค่าได้สวย ๆ:

\`\`\`python
a, b = 1, 2
a, b = b, a        # สลับค่าใน 1 บรรทัด!
print(a, b)        # 2 1
\`\`\`

## เซ็ต (set) — เก็บค่าไม่ซ้ำ

\`\`\`python
nums = {1, 2, 2, 3, 3, 3}
print(nums)         # {1, 2, 3} ซ้ำหายไปเอง
print(len(nums))    # 3
\`\`\`

ตัดค่าซ้ำจากลิสต์ได้ทันที:

\`\`\`python
scores = [80, 95, 80, 60, 95]
unique = set(scores)
print(len(unique))   # 3 → มีคะแนนต่างกัน 3 ค่า
\`\`\`

## ตรวจสมาชิกเร็วมาก

\`\`\`python
vowels = {"a", "e", "i", "o", "u"}
if "e" in vowels:
    print("เป็นสระ")
\`\`\`

## ดำเนินการแบบเซ็ตคณิตศาสตร์

\`\`\`python
a = {1, 2, 3, 4}
b = {3, 4, 5, 6}
print(a & b)    # {3, 4}       intersection — มีทั้งคู่
print(a | b)    # {1,2,3,4,5,6} union — รวมกัน
print(a - b)    # {1, 2}       difference — อยู่ใน a แต่ไม่อยู่ใน b
\`\`\`

## เลือกใช้อะไรดี?

| โครงสร้าง | ลำดับ | แก้ไขได้ | ค่าซ้ำได้ | เหมาะกับ |
|-----------|-------|----------|-----------|----------|
| list | ✓ | ✓ | ✓ | ข้อมูลทั่วไปเรียงลำดับ |
| tuple | ✓ | ✗ | ✓ | ข้อมูลคงที่ |
| set | ✗ | ✓ | ✗ | ตัดซ้ำ / ตรวจสมาชิกเร็ว |
| dict | ✓ | ✓ | key ซ้ำไม่ได้ | จับคู่ key → value |`,
    questions: [
      ["ข้อใดคือความต่างหลักของ tuple กับ list?", ["tuple เร็วกว่าเสมอ", "tuple แก้ไขค่าไม่ได้", "tuple เก็บตัวเลขไม่ได้", "ไม่ต่างกัน"], 1, "tuple เป็น immutable — สร้างแล้วแก้ไม่ได้"],
      ["set ต่างจาก list อย่างไร?", ["set เก็บค่าซ้ำไม่ได้และไม่มีลำดับ", "set แก้ไขไม่ได้", "set เก็บข้อความไม่ได้", "set ช้ากว่า"], 0, "สมาชิกใน set ไม่ซ้ำกันและไม่รับประกันลำดับ"],
      ["len({1, 2, 2, 3, 3}) ได้ค่าอะไร?", ["5", "3", "2", "error"], 1, "ค่าซ้ำถูกตัดเหลือ {1, 2, 3}"],
      ["a, b = b, a ทำอะไร?", ["ลบตัวแปร", "สลับค่าสองตัวแปร", "คัดลอกลิสต์", "เกิด error"], 1, "Python สลับค่าได้ในบรรทัดเดียวด้วย tuple unpacking"],
      ["{1,2,3} & {2,3,4} ได้อะไร?", ["{1,2,3,4}", "{2,3}", "{1,4}", "{1}"], 1, "& คือ intersection — สมาชิกที่มีทั้งสองเซ็ต"],
    ],
  },
  {
    title: "ฟังก์ชัน (Function)",
    content: `# ฟังก์ชัน (Function)

ฟังก์ชันคือกลุ่มคำสั่งที่ตั้งชื่อไว้ เรียกใช้ซ้ำได้ ช่วยให้โค้ดเป็นระเบียบ ไม่ต้องเขียนซ้ำ

\`\`\`python
def greet(name):
    print(f"สวัสดี {name}")

greet("มะลิ")
greet("เพทาย")
\`\`\`

## คืนค่าด้วย return

\`\`\`python
def add(a, b):
    return a + b

result = add(3, 4)
print(result)         # 7
print(add(10, 20))    # เรียกในนิพจน์ได้เลย
\`\`\`

- \`print\` แค่แสดงผล แต่ \`return\` ส่งค่ากลับไปให้ผู้เรียกใช้ต่อได้
- ฟังก์ชันที่ไม่มี return จะคืนค่า \`None\`
- เจอ return เมื่อไหร่ ฟังก์ชันจบทันที

## ค่าเริ่มต้นของพารามิเตอร์

\`\`\`python
def power(base, exp=2):
    return base ** exp

print(power(5))       # 25  (exp ใช้ค่าเริ่มต้น 2)
print(power(2, 10))   # 1024
\`\`\`

## คืนหลายค่าพร้อมกัน

\`\`\`python
def min_max(nums):
    return min(nums), max(nums)

lo, hi = min_max([3, 1, 4, 1, 5])
print(lo, hi)    # 1 5
\`\`\`

## ขอบเขตตัวแปร (scope)

ตัวแปรที่สร้าง**ในฟังก์ชัน**ใช้ได้เฉพาะในฟังก์ชันนั้น:

\`\`\`python
def f():
    x = 10    # ตัวแปรท้องถิ่น (local)

f()
# print(x)    # ❌ error! ข้างนอกมองไม่เห็น x
\`\`\`

## ตัวอย่าง: แยกงานเป็นฟังก์ชัน

\`\`\`python
def is_even(n):
    return n % 2 == 0

def count_evens(nums):
    count = 0
    for n in nums:
        if is_even(n):
            count += 1
    return count

nums = [int(x) for x in input().split()]
print(count_evens(nums))
\`\`\`

โค้ดที่แบ่งเป็นฟังก์ชันเล็ก ๆ อ่านง่าย ทดสอบง่าย และนำกลับมาใช้ใหม่ได้`,
    questions: [
      ["คีย์เวิร์ดใดใช้ประกาศฟังก์ชัน?", ["function", "def", "fn", "func"], 1, "Python ใช้ def"],
      ["คำสั่งใดใช้ส่งค่ากลับจากฟังก์ชัน?", ["print", "give", "return", "send"], 2, "return ส่งค่ากลับไปยังผู้เรียก"],
      ["ฟังก์ชันที่ไม่มี return คืนค่าอะไร?", ["0", "ข้อความว่าง", "False", "None"], 3, "ไม่มี return จะได้ None"],
      ["def f(x, y=5) แล้วเรียก f(3) ค่า y เป็นเท่าไร?", ["3", "5", "0", "error"], 1, "y มีค่าเริ่มต้น 5 เมื่อไม่ส่งมา"],
      ["return ต่างจาก print อย่างไร?", ["ไม่ต่างกัน", "return ส่งค่ากลับให้นำไปใช้ต่อ ส่วน print แค่แสดงผล", "print เร็วกว่า", "return ใช้ได้นอกฟังก์ชัน"], 1, "ค่าที่ return นำไปคำนวณหรือเก็บต่อได้ ส่วน print แสดงอย่างเดียว"],
    ],
  },
  {
    title: "จัดการข้อผิดพลาด (try / except)",
    content: `# จัดการข้อผิดพลาด (try / except)

โปรแกรมที่ดีต้องรับมือกับข้อมูลผิด ๆ ได้โดยไม่พัง เช่น ผู้ใช้พิมพ์ตัวอักษรตอนที่ขอตัวเลข

## ข้อผิดพลาด (error/exception) ที่พบบ่อย

| ชื่อ | เกิดเมื่อ |
|------|-----------|
| ValueError | แปลงค่าไม่ได้ เช่น int("abc") |
| ZeroDivisionError | หารด้วยศูนย์ |
| IndexError | index เกินขนาดลิสต์ |
| KeyError | ไม่มี key ใน dictionary |
| TypeError | ชนิดข้อมูลไม่เข้ากัน เช่น "a" + 1 |

## ดักจับด้วย try / except

\`\`\`python
try:
    age = int(input())
    print(f"ปีหน้าอายุ {age + 1}")
except ValueError:
    print("กรุณาพิมพ์ตัวเลขเท่านั้น")
\`\`\`

ถ้าโค้ดใน \`try\` เกิด error ตามชนิดที่ระบุ โปรแกรมจะกระโดดมาทำใน \`except\` แทนที่จะพัง

## ดักหลายชนิด

\`\`\`python
try:
    a = int(input())
    b = int(input())
    print(a / b)
except ValueError:
    print("ต้องเป็นตัวเลข")
except ZeroDivisionError:
    print("หารด้วยศูนย์ไม่ได้")
\`\`\`

## else และ finally

\`\`\`python
try:
    n = int(input())
except ValueError:
    print("ไม่ใช่ตัวเลข")
else:
    print("แปลงสำเร็จ:", n)     # ทำเมื่อไม่เกิด error
finally:
    print("จบการทำงาน")          # ทำเสมอ ไม่ว่าจะ error หรือไม่
\`\`\`

## ตัวอย่าง: ขอตัวเลขจนกว่าจะถูก

\`\`\`python
while True:
    try:
        n = int(input())
        break
    except ValueError:
        print("ลองใหม่อีกครั้ง")
print("ได้ตัวเลข:", n)
\`\`\`

> ใช้ try/except กับจุดที่**คาดว่า**จะพังจากข้อมูลภายนอก ไม่ใช่ใช้กลบบั๊กของตัวเอง`,
    questions: [
      ['int("hello") เกิด error ชนิดใด?', ["TypeError", "ValueError", "KeyError", "IndexError"], 1, "แปลงข้อความที่ไม่ใช่ตัวเลขเป็น int ไม่ได้ → ValueError"],
      ["บล็อกใดทำงานเมื่อเกิด error ใน try?", ["else", "finally", "except", "catch"], 2, "except ดักจับ error ที่เกิดใน try"],
      ["10 / 0 เกิด error ชนิดใด?", ["ValueError", "ZeroDivisionError", "MathError", "TypeError"], 1, "หารด้วยศูนย์คือ ZeroDivisionError"],
      ["บล็อก finally ทำงานเมื่อไร?", ["เมื่อเกิด error เท่านั้น", "เมื่อไม่เกิด error เท่านั้น", "ทำงานเสมอ", "ไม่มีบล็อกนี้ใน Python"], 2, "finally ทำงานทุกกรณี เหมาะกับงานเก็บกวาด"],
      ["ทำไมไม่ควรใช้ except ดักทุกอย่างแบบกว้าง ๆ?", ["ทำให้โค้ดช้า", "อาจกลบบั๊กจริงจนหาสาเหตุไม่เจอ", "Python ไม่อนุญาต", "ใช้หน่วยความจำมาก"], 1, "ดักกว้างเกินไปทำให้มองไม่เห็นข้อผิดพลาดที่ควรแก้"],
    ],
  },
  {
    title: "โมดูลและไลบรารีมาตรฐาน",
    content: `# โมดูลและไลบรารีมาตรฐาน

Python มี "ไลบรารีมาตรฐาน" ติดมาให้มากมาย — โค้ดสำเร็จรูปที่หยิบมาใช้ได้ด้วยคำสั่ง \`import\`

## วิธี import

\`\`\`python
import math
print(math.sqrt(16))     # 4.0

from math import sqrt, pi
print(sqrt(25), pi)      # 5.0 3.141592653589793
\`\`\`

## โมดูล math

\`\`\`python
import math
math.sqrt(2)       # รากที่สอง
math.floor(3.7)    # 3 ปัดลง
math.ceil(3.2)     # 4 ปัดขึ้น
math.gcd(12, 18)   # 6 ห.ร.ม.
math.pi            # 3.14159...
\`\`\`

## โมดูล random — สุ่ม

\`\`\`python
import random
random.randint(1, 6)              # สุ่มเลข 1-6 (ทอยลูกเต๋า)
random.choice(["หิน", "กระดาษ", "กรรไกร"])  # สุ่มจากลิสต์
random.shuffle(cards)             # สลับลำดับลิสต์
\`\`\`

> โจทย์ในเว็บนี้ตรวจคำตอบแบบเทียบ output ตรง ๆ จึงไม่ใช้ random ในโจทย์ แต่ใช้สนุก ๆ ในเครื่องได้เต็มที่

## โมดูลอื่นที่น่ารู้จัก

| โมดูล | ใช้ทำอะไร |
|-------|-----------|
| datetime | วันที่และเวลา |
| collections | Counter นับความถี่, deque คิว |
| string | ตัวอักษรสำเร็จรูป เช่น a-z |
| json | อ่าน/เขียนข้อมูล JSON |

## ตัวอย่าง: Counter นับความถี่ใน 1 บรรทัด

\`\`\`python
from collections import Counter
print(Counter("banana"))
# Counter({'a': 3, 'n': 2, 'b': 1})
print(Counter("banana").most_common(1))   # [('a', 3)]
\`\`\`

## ก้าวต่อไป

เมื่อคล่องพื้นฐานแล้ว ลองศึกษาไลบรารีภายนอกที่ติดตั้งผ่าน \`pip\` เช่น
- **requests** — ดึงข้อมูลจากเว็บ
- **pandas** — วิเคราะห์ข้อมูลตาราง
- **pygame** — เขียนเกม

ขอให้สนุกกับการเขียนโปรแกรม! 🐍`,
    questions: [
      ["คำสั่งใดใช้เรียกโมดูลมาใช้งาน?", ["include", "import", "require", "using"], 1, "Python ใช้ import"],
      ["math.sqrt(16) ได้ค่าอะไร?", ["4", "4.0", "8.0", "256"], 1, "sqrt คืนค่า float เสมอ จึงได้ 4.0"],
      ["random.randint(1, 6) ให้ค่าแบบใด?", ["1 ถึง 5", "0 ถึง 6", "จำนวนเต็ม 1 ถึง 6", "ทศนิยม 1 ถึง 6"], 2, "randint รวมขอบทั้งสองด้าน"],
      ["math.ceil(3.2) ได้ค่าอะไร?", ["3", "4", "3.5", "3.2"], 1, "ceil ปัดขึ้นเสมอ"],
      ["Counter จากโมดูล collections ใช้ทำอะไร?", ["เรียงลำดับ", "นับความถี่ของสมาชิก", "สุ่มค่า", "แปลงชนิดข้อมูล"], 1, "Counter นับว่าแต่ละค่าปรากฏกี่ครั้ง"],
    ],
  },
];
